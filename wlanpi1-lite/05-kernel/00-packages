# Do not apt-install wlanpi-kernel-bookworm-v8 here.
# packagecloud wlanpi/dev debian/bookworm currently publishes
# 7.1.x whose Debian version is greater than 6.12.x,
# so an unpinned install would pull 7.1. Pin and hold 6.12.107
# (wlanpi-kernel PRs #31 + #37) from the GitHub pre-release in 00-run.sh instead.
# wlanpi-kernel-bookworm-v8
