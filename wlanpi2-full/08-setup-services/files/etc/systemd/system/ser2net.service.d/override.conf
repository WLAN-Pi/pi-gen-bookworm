# ser2net is only needed after switching into wconsole/server/hotspot
# modes; the mode switchers manage it. Cap the stop timeout so a
# hanging ser2net can never stall shutdown or reboot (default is 90s).
[Service]
TimeoutStopSec=5
